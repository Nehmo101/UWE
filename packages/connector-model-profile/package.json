{
  "name": "@uwe/connector-model-profile",
  "version": "0.1.0",
  "private": true,
  "description": "UWE Maschinenraum model profiles — curated metadata (displayName, description, bestFor, modelType, enabledForUwe) and configured scan paths for local model discovery. Types, validation, and key helpers only; persistence lives in the connector tool.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node ../../scripts/run-node-tests.mjs"
  },
  "dependencies": {
    "@uwe/connector": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "@uwe/config": "workspace:*",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
