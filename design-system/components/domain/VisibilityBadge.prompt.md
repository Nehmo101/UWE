Signals content visibility in UWE's player-safety model. `dm_only` ("Nur GM") = terracotta; `player_visible` ("Portal sichtbar") = teal.

```jsx
<VisibilityBadge visibility="dm_only" />
<VisibilityBadge visibility="player_visible" />
```
