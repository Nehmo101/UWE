Small uppercase status badge. Semantic `tone`: neutral · accent · danger · success · warning. For domain statuses (visibility, publish, canon) prefer the domain badges in `components/domain`.

```jsx
<Badge tone="accent">Veröffentlicht</Badge>
<Badge tone="warning">Entwurf</Badge>
```
