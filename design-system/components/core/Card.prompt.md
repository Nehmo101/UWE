Bordered paper panel (design-v2 card). Optional serif `title` and hairline `footer`.

```jsx
<Card title="Nächste Session">
  <p>Session 12 · Freitag 19:00</p>
</Card>
```

Set `padded={false}` for edge-to-edge content (lists, media).
