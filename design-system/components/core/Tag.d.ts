import * as React from "react";

export interface TagProps extends React.HTMLAttributes<HTMLSpanElement> {}

/** Accent-tinted pill for wiki page tags / taxonomy. */
export function Tag(props: TagProps): React.JSX.Element;
