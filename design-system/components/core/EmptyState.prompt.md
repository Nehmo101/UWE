Centered empty placeholder for empty lists/panels.

```jsx
<EmptyState
  icon={<Inbox size={28} />}
  title="Keine Inhalte freigegeben"
  description="Für deine Rolle sind derzeit keine Inhalte sichtbar."
  action={<Button variant="accent">Anfragen</Button>}
/>
```
