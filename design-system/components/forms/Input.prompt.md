Labelled text field. Pass `label` for a stacked label + optional `hint`, or use bare inside your own `<label>`. Import `uweFieldStyle` to style custom fields consistently.

```jsx
<Input label="Titel" placeholder="Neue Seite…" hint="Slug wird automatisch erzeugt." />
```
