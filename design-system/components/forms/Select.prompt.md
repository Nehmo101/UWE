Native select styled to the field system. Pass `options` (strings or `{value,label}`) or your own `<option>` children.

```jsx
<Select label="Sichtbarkeit" options={["Privat", "Nur GM", "Portal sichtbar"]} />
```
