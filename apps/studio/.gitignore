# Build artifact: copied from @uwe/static-export on predev/prebuild (copy:atlas).
/public/atlas/

# Szenenbilder werden aus assets/scenes/ kopiert (scripts/copy-scenes.mjs)
/public/scenes/

# Build-Artefakt: aus terra/ kopiert auf predev/prebuild (copy:terra).
# Gepflegt wird ausschliesslich terra/ — hier NICHTS von Hand aendern.
/public/terra/
